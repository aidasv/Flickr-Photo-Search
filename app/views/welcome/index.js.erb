$('#my-photos').append('<%= j render(partial: "photo", collection: @photos) %>');
<% if @photos.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @photos %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
